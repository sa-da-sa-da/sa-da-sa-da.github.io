/*
 * SheetJS 本地占位（班主任工具备用）
 *
 * 当前版本主要依赖 CDN 加载真正的 XLSX 库（见 class-workbench.html）：
 *   https://cdn.sheetjs.com/xlsx-0.20.2/package/dist/xlsx.full.min.js
 *
 * 此文件作为 CDN 不可达时的备用占位。如需真正的离线 xlsx 导入导出，
 * 请将官方完整版下载到此路径（覆盖本文件）：
 *   curl -L -o vendor/sheetjs.min.js https://cdn.sheetjs.com/xlsx-0.20.2/package/dist/xlsx.full.min.js
 *
 * 注意：若 CDN 和本地文件都不可用，导入将自动降级为 CSV 解析。
 */
if (typeof window !== 'undefined') {
  window.__SHEETJS_PLACEHOLDER__ = true;
  console.warn('[班主任工具] SheetJS 未加载（CDN 与本地均不可用），导入将仅支持 CSV。');
}
