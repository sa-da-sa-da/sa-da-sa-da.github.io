/*
 * SheetJS 本地占位（班主任工具备用）
 *
 * 当前版本已改为使用 CSV 导出（带 UTF-8 BOM，Excel 可直接打开），
 * 不再依赖 XLSX 库。此文件保留以备后续升级真正 xlsx 二进制导出。
 *
 * 若需要真正 xlsx 导出，请替换为官方完整版：
 *   https://cdn.sheetjs.com/xlsx-0.20.2/package/dist/xlsx.full.min.js
 */
if (typeof window !== 'undefined') {
  window.__SHEETJS_PLACEHOLDER__ = true;
}
